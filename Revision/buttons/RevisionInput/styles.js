import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  revisionInput: {
    height: 44,
    width: 300,
    textAlign: "center",
    borderColor: "gray",
    borderWidth: 3,
    borderRadius: 22,
    position: "relative"
  }
});

export default styles;
